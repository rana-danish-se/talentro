# Talentro Server (minimal)

This is a minimal Express app scaffold for the `server` folder.

Commands:

```bash
cd "d:\DB Sql\Talentro\server"
npm install
npm start      # production
npm run dev    # development with nodemon
```

Endpoints:

- `GET /` - basic status JSON
- `GET /api/health` - health check
